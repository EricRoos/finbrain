<div class='flex-row justify-center items-center flex gap-2  bg-gray-100 rounded-full px-4 py-2 tag' id="<%= dom_id tag %>">
  <%= button_to taggable_tag_path(taggable_type: @taggable.class.to_s.underscore, taggable_id: @taggable.id, id: tag.id), class: 'flex-shrink hover:text-red-500', method: :delete do %>
    &times;
  <% end %>
  <div>
    <%= tag.value %>
  </div>
</div>
