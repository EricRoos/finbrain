<p style="color: green"><%= notice %></p>

<h1>Transaction Details</h1>
<hr class='my-4'/>
<% if @bank_transaction.present? %>
  <div class='w-auto'>
    <div>
      <%= link_to "Find another unreviewed (#{BankTransaction.not_reviewed.count})", random_unreviewed_bank_transactions_path, class: 'button button-primary w-auto' %>
      <hr class='my-4'/>
    </div>
  </div>
  <%= render @bank_transaction %>
<% end %>
